// If reservation has drop off at another location checked,
// then if it is unchecked, set the drop off location to the pick up location id
document.addEventListener('DOMContentLoaded', function(event) {
    // Format time for hidden inputs here
    /*if (document.getElementById('pickuptimeformat')) {
        const startingPickUp = document.getElementById('pickuptimeformat').value;
        const pickUpYear = startingPickUp.substring(9, 13);
        const pickUpMonth = getMonth(startingPickUp.substring())
        console.log(pickUpYear);
        console.log(startingPickUp.slice(-4))
        if (startingPickUp.slice(-3, 0) === 'p.m.') {
            // Add 12 to hours
        }
    }
    const startingDropOff = document.getElementById('dropofftimeformat').value;*/
});